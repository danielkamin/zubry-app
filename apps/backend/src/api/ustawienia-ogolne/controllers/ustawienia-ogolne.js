'use strict';

/**
 * ustawienia-ogolne controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::ustawienia-ogolne.ustawienia-ogolne');
