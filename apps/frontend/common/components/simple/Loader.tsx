import { LoadingOutlined } from '@ant-design/icons';

const Loader = () => {
  return (
    <div className="w-full flex justify-center items-center text-primary text-4xl">
      <LoadingOutlined />
    </div>
  );
};

export default Loader;
